# AI ADR Index

Canonical ADRs live in ../../docs/decisions/.

Use this index when an AI assistant needs a short decision map:

- 0001: devices are modeled by capabilities, not names
- 0002: normalized `InputEvent` is the core data model
- 0003: vendor SDK material is read-only
- 0004: initial plugins are in-process
- 0005: recording and replay are first-class
- 0006: restricted PCs should be served through a hardware HID bridge strategy

When adding or changing an architectural decision, update the canonical ADR first and this index second.
